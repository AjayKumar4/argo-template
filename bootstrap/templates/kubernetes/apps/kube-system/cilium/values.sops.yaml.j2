#% if bootstrap_cloudflare.enabled %#
domain: "hubble.#{ bootstrap_cloudflare.domain }#"
#% endif %#
ipv4_cidr: "#{ bootstrap_pod_network.split(',')[0] }#"
#% if bootstrap_feature_gates.dual_stack_ipv4_first %#
ipv6_cidr: "#{ bootstrap_pod_network.split(',')[1] }#"
#% endif %#
